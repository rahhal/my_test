{% extends 'base.html.twig' %}

{% block title %}Neighborhood{% endblock %}

{% block body %}
    <h1>Neighborhood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ neighborhood.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ neighborhood.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('neighborhood_index') }}">back to list</a>

    <a href="{{ path('neighborhood_edit', {'id': neighborhood.id}) }}">edit</a>

    {{ include('neighborhood/_delete_form.html.twig') }}
{% endblock %}
